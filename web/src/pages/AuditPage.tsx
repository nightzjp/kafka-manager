import { useEffect,useState } from 'react';import { api } from '../lib/api';import { AuditEntry } from '../lib/types';import { Empty,ErrorNotice,Loading,PageHeader } from '../components/Common';
export function AuditPage(){const[items,setItems]=useState<AuditEntry[]>([]);const[loading,setLoading]=useState(true);const[error,setError]=useState('');const load=()=>{setLoading(true);api.get<{items:AuditEntry[]}>('/api/v1/audit?limit=200').then(r=>{setItems(r.items);setError('')}).catch(e=>setError(e.message)).finally(()=>setLoading(false))};useEffect(load,[]);return <><PageHeader code="06 / AUDIT" title="操作时间线" description="按日期目录持久化的管理操作记录，不保存消息正文和认证凭证。" actions={<button className="button ghost" onClick={load}>刷新日志 ↻</button>}/>{error&&<ErrorNotice message={error}/>} {loading?<Loading/>:items.length===0?<Empty title="暂无操作记录" detail="创建 Topic、生产消息或重置 Offset 后会在这里留下记录。"/>:<div className="timeline">{items.map((entry,index)=><article key={`${entry.timestamp}-${index}`}><time>{new Date(entry.timestamp).toLocaleString()}</time><span className={entry.result==='success'?'success':'failure'}/><div><b>{entry.action}</b><code>{entry.clusterId||'system'} / {entry.resource||'-'}</code>{entry.error&&<p>{entry.error}</p>}</div><strong>{entry.result.toUpperCase()}</strong></article>)}</div>}</>}
