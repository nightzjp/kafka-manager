import { FormEvent,useDeferredValue,useEffect,useState } from 'react';import { api } from '../lib/api';import { Topic } from '../lib/types';import { Dialog,Empty,ErrorNotice,Loading,PageHeader,Status } from '../components/Common';import { Icon } from '../components/Icon';
export function TopicsPage({clusterId,onOpen}:{clusterId:string;onOpen:(topic:Topic,tab?:'overview'|'messages')=>void}){const[items,setItems]=useState<Topic[]>([]),[search,setSearch]=useState(''),[loading,setLoading]=useState(true),[error,setError]=useState(''),[creating,setCreating]=useState(false);const deferred=useDeferredValue(search);const load=()=>{if(!clusterId)return;setLoading(true);api.get<{items:Topic[]}>(`/api/v1/clusters/${clusterId}/topics?search=${encodeURIComponent(deferred)}&pageSize=200`).then(r=>{setItems(r.items);setError('')}).catch(e=>setError(e.message)).finally(()=>setLoading(false))};useEffect(load,[clusterId,deferred]);return <><PageHeader code="TOPICS" title="Topic 工作台" description="查看健康状态，进入 Topic 后直接读取消息、检查分区和修改配置。" actions={<button className="button primary" onClick={()=>setCreating(true)}><Icon name="plus"/>创建 Topic</button>}/><div className="toolbar"><label className="search-field"><Icon name="search"/><input aria-label="搜索 Topic" placeholder="按名称筛选 Topic" value={search} onChange={e=>setSearch(e.target.value)}/></label><span>{items.length} 个 Topic</span></div>{error&&<ErrorNotice message={error}/>} {loading?<Loading/>:items.length===0?<Empty title="没有匹配的 Topic" detail="调整搜索条件，或创建第一个 Topic。"/>:<div className="data-table"><table><thead><tr><th>Topic</th><th>健康状态</th><th>分区</th><th>复制因子</th><th>操作</th></tr></thead><tbody>{items.map(topic=><tr key={topic.Name}><td><div className="topic-name"><span className="topic-glyph">T</span><div><button className="topic-link" onClick={()=>onOpen(topic)}>{topic.Name}</button><small>{topic.Internal?'系统 Topic':'业务 Topic'}</small></div></div></td><td>{topic.UnderReplicated?<Status tone="bad">{topic.UnderReplicated} 个 ISR 异常</Status>:<Status tone="good">健康</Status>}</td><td className="mono">{topic.PartitionCount}</td><td className="mono">{topic.ReplicationFactor}</td><td><div className="row-actions"><button aria-label={`复制 ${topic.Name}`} onClick={()=>navigator.clipboard?.writeText(topic.Name)} title="复制名称"><Icon name="copy"/></button><button onClick={()=>onOpen(topic,'messages')}>查看消息 <Icon name="arrow"/></button></div></td></tr>)}</tbody></table></div>}{creating&&<CreateTopic clusterId={clusterId} close={()=>setCreating(false)} saved={()=>{setCreating(false);load()}}/>}</>}
function CreateTopic({clusterId,close,saved}:{clusterId:string;close:()=>void;saved:()=>void}){const[error,setError]=useState('');async function submit(e:FormEvent<HTMLFormElement>){e.preventDefault();const data=new FormData(e.currentTarget);try{await api.post(`/api/v1/clusters/${clusterId}/topics`,{name:data.get('name'),partitions:Number(data.get('partitions')),replicationFactor:Number(data.get('replication'))});saved()}catch(reason){setError(reason instanceof Error?reason.message:'创建失败')}}return <Dialog title="创建 Topic" onClose={close}><form className="dialog-body form" onSubmit={submit}><label>Topic 名称<input name="name" required placeholder="orders.v1" autoFocus/></label><div className="form-row"><label>分区数<input name="partitions" type="number" min="1" defaultValue="3"/></label><label>复制因子<input name="replication" type="number" min="1" defaultValue="1"/></label></div>{error&&<ErrorNotice message={error}/>}<div className="form-actions"><button type="button" className="button ghost" onClick={close}>取消</button><button className="button primary">创建 Topic</button></div></form></Dialog>}
